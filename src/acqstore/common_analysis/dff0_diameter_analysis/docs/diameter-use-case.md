# Diameter Use Case

Reporter onset indices from Sum Intensity peak detection seed analysis of raw diameter values. Diameter is filtered locally by the selected generic filter.

For contraction-like responses, `EventDirection.NEGATIVE` identifies the minimum diameter as the extremum. For dilation-like responses, `EventDirection.POSITIVE` identifies the maximum.

The code measures temporal order and association. It does not establish biological causality.
